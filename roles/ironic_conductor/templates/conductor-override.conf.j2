# Per-conductor instance configuration override
# Loaded AFTER the main ironic.conf; values here take precedence.

[DEFAULT]
host = ironic-conductor-{{ item.name }}

[conductor]
workers_pool_size = {{ item.workers | default(ironic_conductor_default_workers) }}
conductor_group = {{ item.name }}
